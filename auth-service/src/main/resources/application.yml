spring:
  application:
    name: auth-service

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      show-details: always

internal-user-web-client:
  base-url: "http://user-service/internal/v1/users"

auth:
  server:
    issuer-uri: "http://dev.proptech.com"