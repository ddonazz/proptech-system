spring:
  application:
    name: mail-service
  rabbitmq:
    host: rabbitmq
    port: 5672

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      show-details: always