package com.andrea.proptech.property.property.data;

public enum PropertyType {
    BUILDING,   // Un condominio, palazzina
    VILLA,      // Una villa singola (potrebbe avere 1 o più unità)
    COMPLEX,    // Un complesso residenziale (es. "Residence Le Palme")
    LAND        // Terreno (per sviluppi futuri)
}
